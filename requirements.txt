# Core - use specific versions with pre-built wheels
streamlit==1.52.2
pandas==2.1.4  # Older but stable version with pre-built wheels
numpy==1.26.4  # Stable numpy version
plotly==5.18.0
altair==5.2.0
